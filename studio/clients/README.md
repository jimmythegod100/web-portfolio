# clients/

Generated client projects live here (gitignored).

```bash
cd ../   # studio/
make new-client NAME=your-client-slug
```

Each folder is a full Docker stack: frontend + FastAPI + optional Postgres.
